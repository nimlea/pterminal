# coding:utf-8
#! python3

from setuptools import setup, find_packages

setup(
        name = 'pipe-terminal',
        version = '0.1',
        description = 'An utility input/output library for terminal in new thread',
        author = 'YL',
        url = 'https://github.com/yyyyl/pipe-terminal',
        packages = find_packages(),
)